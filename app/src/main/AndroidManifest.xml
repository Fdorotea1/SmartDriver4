<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.smartdriver">

    <!-- Permissões Essenciais -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- Bateria (para o passo do wizard) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Permissões de Armazenamento (Opcional/Legado) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartDriver"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAiVQyn3l3wYP6f28Juk7dBgZJKbFe0TIs"/>

        <!-- Diálogo/meta -->
        <activity
            android:name=".SetShiftTargetActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartDriver.TransparentDialog"
            android:launchMode="singleTop" />

        <activity
            android:name=".map.MapEditorActivity"
            android:exported="true"
            android:label="@string/map_editor_title" />

        <!-- Splash (ÚNICO LAUNCHER) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartDriver.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MAIN com barra preta -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.SmartDriver.BlackBar" />

        <!-- SETTINGS com barra preta -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.SmartDriver.BlackBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <!-- Histórico -->
        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:label="Histórico"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <!-- Preview de Mapa (Google Maps) - sem LAUNCHER -->
        <activity
            android:name=".map.MapPreviewActivity"
            android:exported="false"
            android:label="Mapa (Google)"
            android:launchMode="singleTop"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
            android:theme="@style/Theme.SmartDriver.MapOverlay" />

        <!-- Wizard de permissões (primeira execução) -->
        <activity
            android:name=".permissions.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartDriver.BlackBar" />

        <!-- Centro de Permissões (acesso manual após onboarding) -->
        <activity
            android:name=".permissions.PermissionsCenterActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartDriver.BlackBar" />

        <!-- Serviços -->
        <service
            android:name=".SmartDriverAccessibilityService"
            android:exported="false"
            android:label="SmartDriver Service"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".ScreenCaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <service
            android:name=".overlay.OverlayService"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>
